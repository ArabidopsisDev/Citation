<UserControl
    x:Class="Citation.View.Page.DetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:Citation.Model.Convert"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <c:AuthorConverter x:Key="AuthorsConverter" />
        <c:DateConverter x:Key="DateConverter" />
    </UserControl.Resources>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <Image
            Name="GoBack"
            Width="32"
            Height="32"
            Margin="20,0,0,0"
            HorizontalAlignment="Left"
            MouseDown="GoBack_MouseDown"
            Source="pack://application:,,,/Citation;component/Images/back.png" />

        <TextBlock
            Grid.Row="0"
            Margin="0,30,0,20"
            HorizontalAlignment="Center"
            FontSize="30"
            FontWeight="DemiBold"
            Text="引用详细信息" />

        <Border
            Grid.Row="1"
            Width="700"
            Margin="0,0,0,20"
            HorizontalAlignment="Center"
            VerticalAlignment="Top">
            <Border
                Padding="25"
                Background="White"
                BorderBrush="LightGray"
                BorderThickness="1"
                CornerRadius="15">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock
                            Margin="0,5,0,2"
                            FontWeight="Bold"
                            Text="标题:" />
                        <TextBox
                            Margin="0,0,0,15"
                            Padding="0,5"
                            BorderBrush="LightGray"
                            BorderThickness="0,0,0,1"
                            FontFamily="Calibri"
                            FontSize="16"
                            Text="{Binding Article.Message.Title[0]}" />

                        <TextBlock
                            Margin="0,5,0,2"
                            FontWeight="Bold"
                            Text="作者:" />
                        <TextBox
                            Margin="0,0,0,15"
                            Padding="0,5"
                            BorderBrush="LightGray"
                            BorderThickness="0,0,0,1"
                            FontFamily="Calibri"
                            FontSize="16"
                            Text="{Binding Article.Message.Author, Converter={StaticResource AuthorsConverter}}" />

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock
                                    Margin="0,5,0,2"
                                    FontWeight="Bold"
                                    Text="期刊名称:" />
                                <TextBox
                                    Padding="0,5"
                                    BorderBrush="LightGray"
                                    BorderThickness="0,0,0,1"
                                    FontFamily="Calibri"
                                    FontSize="16"
                                    Text="{Binding Article.Message.Container[0]}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock
                                    Margin="0,5,0,2"
                                    FontWeight="Bold"
                                    Text="发表日期:" />
                                <DatePicker
                                    Padding="0,5"
                                    BorderBrush="LightGray"
                                    BorderThickness="0,0,0,1"
                                    FontFamily="Calibri"
                                    FontSize="16"
                                    SelectedDate="{Binding Article.Message.Published, Converter={StaticResource DateConverter}}" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock
                                    Margin="0,5,0,2"
                                    FontWeight="Bold"
                                    Text="卷号:" />
                                <TextBox
                                    Padding="0,5"
                                    BorderBrush="LightGray"
                                    BorderThickness="0,0,0,1"
                                    FontFamily="Calibri"
                                    FontSize="16"
                                    Text="{Binding Article.Message.Volume}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock
                                    Margin="0,5,0,2"
                                    FontWeight="Bold"
                                    Text="期号:" />
                                <TextBox
                                    Padding="0,5"
                                    BorderBrush="LightGray"
                                    BorderThickness="0,0,0,1"
                                    FontFamily="Calibri"
                                    FontSize="16"
                                    Text="{Binding Article.Message.Issue}" />
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock
                                    Margin="0,5,0,2"
                                    FontWeight="Bold"
                                    Text="页码:" />
                                <TextBox
                                    Padding="0,5"
                                    BorderBrush="LightGray"
                                    BorderThickness="0,0,0,1"
                                    FontFamily="Calibri"
                                    FontSize="16"
                                    Text="{Binding Article.Message.Page}" />
                            </StackPanel>
                        </Grid>

                        <TextBlock
                            Margin="0,5,0,2"
                            FontWeight="Bold"
                            Text="DOI:" />
                        <TextBox
                            Margin="0,0,0,15"
                            Padding="0,5"
                            BorderBrush="LightGray"
                            BorderThickness="0,0,0,1"
                            FontFamily="Calibri"
                            FontSize="16"
                            Text="{Binding Article.Message.Doi}" />

                        <TextBlock
                            Margin="0,5,0,2"
                            FontWeight="Bold"
                            Text="保存文件夹:" />
                        <TextBox
                            Margin="0,0,0,15"
                            Padding="0,5"
                            BorderBrush="LightGray"
                            BorderThickness="0,0,0,1"
                            FontFamily="Calibri"
                            FontSize="16"
                            Text="{Binding Article.Message.Folder}" />

                        <TextBlock
                            Margin="0,5,0,2"
                            FontWeight="Bold"
                            Text="URL:" />
                        <TextBox
                            Margin="0,0,0,15"
                            Padding="0,5"
                            BorderBrush="LightGray"
                            BorderThickness="0,0,0,1"
                            FontFamily="Calibri"
                            FontSize="16"
                            Text="{Binding Article.Message.Url}" />

                        <TextBlock
                            Margin="0,5,0,2"
                            FontWeight="Bold"
                            Text="摘要:" />
                        <TextBox
                            MinHeight="100"
                            Padding="5"
                            AcceptsReturn="True"
                            Background="#FAFAFA"
                            BorderBrush="LightGray"
                            BorderThickness="1"
                            FontFamily="Calibri"
                            FontSize="16"
                            IsReadOnly="True"
                            Text="{Binding Article.Message.Abstract}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Border>

        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Orientation="Horizontal">
            <Button
                Width="120"
                Height="40"
                Margin="0,0,20,0"
                Background="#0078D7"
                BorderThickness="0"
                Click="SaveButton_Click"
                Content="保存"
                FontSize="16"
                FontWeight="Medium"
                Foreground="White">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="20">
                            <ContentPresenter
                                x:Name="contentPresenter"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button
                Width="120"
                Height="40"
                Background="LightGray"
                BorderThickness="0"
                Click="CancelButton_Click"
                Content="取消"
                FontSize="16"
                FontWeight="Medium"
                Foreground="Black">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="20">
                            <ContentPresenter
                                x:Name="contentPresenter"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>